# Progress — Portfolio

## Graphics development plan — approved 2026-07-31

The next visual development pass will add eight related but distinct graphic treatments, in this order:

1. Homepage — Practice relay (`WORK → SYSTEMS → PROVENANCE`)
2. Security + Systems — Trust-boundary topology
3. Web Development — Render pipeline (`DESIGN → IMPLEMENT → TEST → CONTAIN`)
4. Proof Sheet — Local/remote data-boundary plate
5. PROVENANCE index — Chain-of-custody ribbon
6. PROVENANCE index — Specimen status marks
7. First Known Use — Archive layer map
8. 404 — Broken route trace

Each treatment is an independent, reversible slice. As the slices are built, record the reusable rules for their graphic family—geometry, line weight, node anatomy, signal behavior, color semantics, responsive composition, accessibility, and reduced-motion behavior—before reusing that family elsewhere. The initial shared assumption is a small inline SVG or semantic HTML/CSS system with no new dependency; a heavier runtime needs separate justification.

### Deferred docket

After the Security + Systems visualizations are finished and reviewed, return to PROVENANCE for two more abstract treatments:

- **Forked History:** one source branching into divergent artifact histories, with completed, unresolved, and abandoned paths.
- **Negative-Space Archive:** missing evidence represented as quiet gaps that resolve only as provenance becomes available.

Keep the current custody ribbon as the interim Provenance visualization until that review point.

The first slice is the homepage Practice relay. It should remain a quiet explanatory instrument: one signal, three labeled stages, responsive reflow, keyboard-safe surrounding content, an accessible text equivalent, and a static reduced-motion state.

### Graphics refinement pass — active

The first-pass graphics are implemented and are now being refined one at a time from visual review. Homepage Practice relay refinement is complete for this pass; its receiver timing now runs in explicit `Work → Systems → Provenance` order, and its separate transparent carrier/point-glow geometry follows the original home-lab paint-order principle so the rail hides the moving line. Halo variable 01 was rejected as a visible line; variables 02/03/04 are the accepted current halo stack. The Web Development render pipeline is now on the same measured signal controller, with circular receiver rings restored, transparent resting centers, expanding inner blue orbs, endpoint timing tuned independently, a finite directional sequence, Earthsong blue active state, and responsive horizontal-to-vertical reflow. The Provenance custody ribbon is now on the same controller with archival rectangular receivers, a green evidence signal, a readable endpoint hold, and vertical mobile reflow. The Security + Systems topology now describes identity-based access into a private boundary, with services and storage as destinations and DNS filtering plus isolated testing as controlled branches. The home-lab loops now use one shared circular-progress clock, centered glow layers, synchronized receiver dots, a shared Recover checkpoint, and a static reduced-motion state. The Proof Sheet boundary plate now separates a synchronized orange local pipeline from a subordinate green remote model-weight tether into the Worker, with hidden cores, centered halo/bloom layers, and synchronized receiver dots. The First Known Use archive-layer graphic was the previously omitted screenshot.

### Practice relay / pipeline-family rules established

- Use a semantic ordered list as the source of truth; the line and moving signal are explanatory layers, not the content itself.
- Keep the rail at one pixel and make the active signal narrow; glow carries atmosphere without increasing the apparent line weight.
- Use one warm active signal and neutral receivers. A stage should not appear active merely because it is present.
- Use compact indices and short plain-language descriptors so the graphic remains legible as editorial content.
- Reflow the pipeline vertically on narrow screens rather than scaling a wide composition into a clipped viewport.
- The signal pauses as a static accent under `prefers-reduced-motion: reduce`; the sequence remains fully readable without animation.

### Trust-boundary topology / topology-family rules established

- Show conceptual roles and trust zones, never private addresses, hostnames, credentials, or targetable topology.
- Distinguish the identity/access entry point, private destinations, and control or isolation branches; do not imply that every node is one serial request path.
- Use a quiet dashed enclosure for a boundary and solid or dashed routes to distinguish primary flow from side branches.
- Reserve the active color for access or an observed signal; keep passive nodes neutral and isolate special branches with green.
- Use a single directional signal on the primary route. Branches explain structure but do not compete with the signal.
- Pair the map with plain-language surrounding text and a caption that states the abstraction boundary.
- Keep topology graphics as inline SVG with a real title and description, and freeze the signal under reduced motion.
- Layer the primary route signal into a quiet field, a soft halo, and a one-pixel carrier; keep branch routes passive.
- Light only the receivers on the directional route, in order, so access is legible without exposing operational detail.
- Drive the route layers and receiver states from one normalized progress clock; never assign independent negative animation delays to sequential nodes.
- Use the route's measured segment lengths for receiver progress, then derive branch-carrier progress from the junction where each branch leaves the primary route.
- Branch carriers use the green semantic accent and expand only the inner receiver dot on arrival; branch routes remain quiet until their primary route junction is reached.
- Paint the muted route after the signal layers so the one-pixel carrier disappears into the rail while the halo and bloom remain visible; omit the broadest field layer and hide the core when either reads as a second line.
- Treat the subnet-router pause as a conceptual audit checkpoint, not literal packet timing; label that distinction in the accessible caption.

### Shared recovery loops / loop-family rules established

- Use one circular progress clock for both operational-use and provenance loops.
- Keep the shared `Recover` object as the single checkpoint between the loops.
- Center each SVG glow layer using its own dash length; do not offset every layer by the same amount.
- Keep the broadest field layer out when it reads as a second rail; let trail, halo, and bloom carry the motion.
- Hide the one-pixel core and use a thin, centered trail/halo/bloom stack so glow, not line weight, communicates movement.
- Drive receiver dots and the shared recovery state from progress, with Recover lighting only as the carriers cross and a quiet static state under reduced motion.

### Render pipeline / pipeline-family rules established

- Use a semantic ordered sequence as the source of truth; the rail is a visual index of the stages.
- Use compact rectangular receivers instead of circular loop nodes so sequence graphics remain distinct from operational loops.
- Keep the pipeline directional and finite. It should not imply that every project returns to the beginning automatically.
- Use one active signal and choose its color by meaning; this pipeline uses Earthsong blue for an active browser/render state while passive stages stay neutral.
- Reflow to a vertical rail on mobile with the same stage order and no clipped horizontal canvas.
- Measure receiver centers after layout and move the signal between the first and last receiver; do not animate against guessed container edges.
- Derive receiver lighting from the same normalized progress value as the signal so the signal and node state cannot drift apart.
- Keep the green signal quiet and narrow: the rail stays structural while the halo and bloom carry the sense of motion.

### Boundary plate / boundary-family rules established

- Draw the local boundary as a quiet enclosure and show only the dependency that actually crosses it.
- Make the data path directional and explicit; the remote path must never visually suggest that private input leaves the local boundary.
- Use the caption and surrounding prose to state the privacy model in plain language; the graphic is reinforcement, not the sole explanation.
- Keep the remote dependency visually subordinate to the local pipeline, with a distinct dashed route and green receiver.
- Preserve a static path under reduced motion and retain the full text explanation beside the plate.

### Archive ribbon and specimen marks / archive-family rules established

- Use a finite three-stage ribbon—source, condition, artifact—to describe an experiment without implying a universal workflow.
- Keep the archive ribbon quieter than the specimen content; it should orient the reader, not become a second hero graphic.
- Use small status marks as descriptive metadata, not product-style badges or claims of completeness.
- Keep status words concrete and pair them with an accessible sentence so color and position never carry meaning alone.
- Allow the ribbon to become a vertical trace on mobile while preserving stage order and readable labels.

### Archive layers and broken route / trace-family rules established

- Use offset layers for archival material and let a single registration line suggest connection across them.
- Keep archive graphics materially specific to their project; they should not inherit the systems topology vocabulary.
- For recovery/error surfaces, show the missing connection as a designed gap rather than inventing a false destination.
- Keep the recovery action as ordinary semantic content beside the trace, with the graphic never becoming the only route home.

## Status

- First vertical slice implemented: minimal Astro 7 shell, First Known Use project route, and isolated native-JS word-field artifact.
- Production build passes; the reported word-field overlap was fixed by replacing fragile absolute positioning with a responsive wrapping layout.
- Added one public First Known Use fish graphic as a route-local visual anchor with descriptive alt text and caption; it is now constrained inside a local responsive frame, with no broader visual system introduced.
- Expanded the route into a compact case study with project-specific contribution, stack, content, editorial context, and process narrative.
- Applied JetBrains Mono and the Earthsong terminal palette as the current reversible visual direction: dark brown ground, warm parchment text, and restrained cream/green/orange accents.
- Reorganized work into independently routed sections for web development, audio, networking, and fabrication; First Known Use now lives under web development, with the former route preserved as a redirect.
- Added a named Astro view transition for the First Known Use preview window; the more experimental portal-style click effect was intentionally removed after it did not produce the desired result.
- Moved the interactive word field out of First Known Use and into the web-development section as its own experiment. Its inline script reinitializes after client-side navigation.
- Expanded the networking section description to include security and renamed the woodworking section to fabrication.
- Swapped the shared navigation order so Networking appears before Audio.
- Replaced the web-development First Known Use preview card image with a live iframe of `firstknownuse.com`; the local case-study visual remains unchanged.
- Suppressed the live preview iframe scrollbar so the project window reads as a contained visual preview; full scrolling remains available through the external-site link.
- Reviewed the live First Known Use WIP and refreshed its portfolio case study to reflect the current archive content, monochrome/fluorescent visual language, playful controls, GIFs, doodles, and WIP status; the preview window is labeled accordingly.
- Added Proof Sheet as an independently authored web-development experiment with a project card, case-study route, browser-local image pipeline description, inference/runtime details, and fallback behavior.
- Added a working Proof Sheet artifact to the project route: local image selection, drag/drop/paste input, seeded deterministic dithered composition, generated callouts, recomposition, and 2000px PNG download.
- Replaced the embedded generated-only callouts with the documented browser DETR object detector in a module worker, using WebGPU when available, WASM otherwise, and retaining deterministic geometry fallback; constrained the on-page preview while preserving the 2000px download.
- Reviewed Montana's generated proof sheets and confirmed they were showing deterministic fallback labels/positions. Corrected the embedded pipeline to run DETR against the clean source canvas before dithering, matching standalone Proof Sheet's detection/composition order.
- Follow-up diagnosis found the fallback persisted; updated the worker to pass Transformers.js `RawImage.fromCanvas()` input, lowered the DETR threshold to 0.2, and added an explicit detected-object count to the live status.
- Console review identified the final blocker: no WebGPU adapter was correctly falling through to WASM, but the ONNX Runtime WASM path was unresolved. The worker now checks for an actual adapter and explicitly uses the matching ONNX Runtime WASM CDN assets.
- The newest test confirmed real DETR inference was running, but its returned box units were being interpreted incorrectly and clustered at the upper-left corner. Added unit-aware mapping for normalized, percentage, and pixel coordinate formats.
- Compared the two latest DETR outputs with standalone Proof Sheet and aligned the embedded flow further: local border masking now precedes detection, and detections are score-sorted, overlap-suppressed, and limited to seeded top-K regions before composition.
- Replaced the stale inline Proof Sheet runtime with a route-local pipeline matching the current source architecture: `removeBackground()`, `regions()` and its OWLv2 worker, `dither()`, `compose()`, grain, and the existing JetBrains Mono font package. The detector contract is now source-image pixels, clamped before composition; fallback output is explicitly marked as geometry fallback.
- Added robust image, canvas, ImageBitmap, worker, model, and stale-render handling; file selection, drag/drop, active-tool paste, keyboard activation, seed validation, 2000×2000 PNG output, and nested-route asset URLs are covered. OWLv2 weights remain a remote first-use download; ONNX WASM runtime assets and grain are emitted as route build assets.
- Changed the Proof Sheet result preview to flow inline without an inner scrollbar; its dynamic image now receives the scoped Astro styling via a global descendant selector, left-aligning with the controls above at up to 38rem while the generated 2000×2000 PNG object URL remains the download source.
- Lowered the OWLv2 retention threshold from 0.25 to 0.15 for both WebGPU and WASM so successful inference is more likely to retain useful visual callouts instead of producing a zero-detection sheet.
- Broadened Proof Sheet detection to the complete vocabulary, lowered the OWLv2 threshold to 0.03, and increased retained regions from 1–4 to 5–7 for denser small-object callouts; overlap suppression and the large-region guard remain in place.
- Changed the image dither from monochrome thresholding to seeded four-level-per-channel color quantization, and cleared/clipped the composition grain so the outermost 90px border exports transparent in the PNG.
- Made detection caching seed-aware: recomposition with the same seed can reuse inference, while a new seed or Recompose request runs OWLv2 again against the cached masked source.
- Adopted the Montana Kit role-based font set sitewide: self-hosted Archivo body/UI, Departure Mono system voice, JetBrains Mono microtype, and the remaining kit faces/OFL licenses are available as shared assets; Proof Sheet UI and generated canvas type use the mono role.
- Switched the global inherited body/UI font to the kit's Departure Mono role as the current reversible sitewide direction.
- Restored JetBrains Mono as the inherited sitewide font and Proof Sheet type; the Montana Kit font assets and role variables remain installed for future special-case use.
- Added a delayed terminal-style type-in for the Departure Mono `proof-sheet` artifact heading, with a blinking block cursor, regular weight, and reduced-motion instant fallback.
- Tried the document-header treatment for the page-level title: `PROOF SHEET` with `PROVENANCE / 01` aligned to the right and a browser-experiment metadata line below; replaced the visible `PS / 01` mark with `PROVENANCE / 01`.
- Restored the stamped title treatment with `[ PROVENANCE / 01 ]` above `PROOF SHEET`, moved the delayed terminal type-in and cursor to the page title, and removed the lower artifact heading/type-in.
- Renamed the visible Proof Sheet provenance identifier from `01` to `00` and replaced the page’s `montana-kit` reference with `PROVENANCE`.
- Reduced the option 4 page-title scale to keep the stamped header prominent without dominating the page.
- Extended the page-title typewriter into a two-stage CLI sequence: `[ PROVENANCE / 00 ]`, then `PROOF SHEET`, with the block cursor handed from the stamp to the title.
- Aligned the Proof Sheet controls and generated result to the page content edge by removing their extra horizontal inset; the drop-zone padding remains intentionally spacious.
- Removed the drop-zone’s horizontal padding so its provenance mark, input prompt, and controls now share the same page content edge.
- Limited file-picker activation to the Choose Image button; drag/drop and paste remain supported, and the button now uses a raised/pressed analog control treatment.
- Refined Choose Image into a tactile switch-style control with a square cap, beveled edges, inset rim, and pressed state based on the provided hardware reference.
- Strengthened the tactile button styling with native-appearance reset and a visible beveled gradient face after the first visual check showed no apparent change.
- Corrected the tactile button specificity so its scoped styles override the shared button rule; the previous class-only selector was losing to Astro’s scoped ancestor-plus-element selector.
- Refined the Choose Image control into a more realistic hardware switch with a molded gradient cap, metallic bevel, inset rim, layered depth, contact shadow, hover highlight, and compressed active state.
- Added a final realism pass to the switch: curved specular highlight, multi-stage molded shading, recessed seam, tighter bevel, and softened physical shadow.
- Added subtle material grain and layered highlight response to the switch face so the control reads more like a manufactured object than a flat UI element.
- Increased the switch material contrast after visual review: stronger grain, deeper lower-edge shadow, and a visible curved specular strip now survive the rendered button scale.
- Recast Choose Image as an archival instrument control inspired by the supplied specimen sheets: a technical `INPUT / 00` label, registration-like divider, colored status lamp, and tactile painted face.
- Reimagined Choose Image as an alien archival input sigil: a neon glyph field, grid substrate, specimen label, and pressed instrument-panel behavior replace the conventional hardware-button language while retaining an accessible native button.
- Removed the literal Choose Image label from the sigil, promoted `INPUT` to the action line, moved `00 // LOCAL` to the footer, replaced the orderly glyph row with individually rotated/scaled marks, and changed the accent to Earthsong green.
- Reworked the sigil into one-bit Earthsong-orange mode: stranger irregular glyphs, no grid or soft glow, `INPUT` typeout with a blinking CLI cursor, and `00 // LOCAL` right-aligned on the same line; the lower line is removed.
- Restored the orange grid substrate and glow, and made the one-bit glyph field generate a fresh random constellation on each page load with varied symbols, rotations, and scales.
- Replaced the generated symbols with 3×3 bitmap glyphs whose pixels are 1–9px, regenerating the constellation every second with reduced-motion and navigation cleanup.
- Fixed dynamic bitmap styling through global descendant selectors and added a 4×8 background field of independently randomized 3×3 bitmap tiles that regenerates with the glyphs each second.
- Standardized the top glyph row to unrotated, uniform-size 3×3 marks; each glyph now randomizes independently on a one-second interval after its own randomized 0–1 second start delay, while the background tile field remains unchanged.
- Extended the independent randomized timing to every background 3×3 tile, tightened the glyph-to-input spacing, and grouped `INPUT` with its cursor so the CLI effect stays attached to the typed text.
- Reduced the background bitmap field from four rows to three so the control is shorter, removed spacing between bitmap cells and glyphs for a continuous strip, and changed the top glyphs to bright white with a substantially stronger layered glow.
- Added directional softening to background pixels at empty-space boundaries, moved the top glyph glow onto the actual active pixels so it remains visible, and bolded `INPUT` plus `00 // LOCAL` for readability.
- Refined background edge softening to round only corners associated with each bit’s actual empty neighbors, and strengthened the independent label glows for `INPUT` and `00 // LOCAL`.
- Removed bitmap edge softening entirely and replaced density-based generation with the requested weighted 1–9 active-pixel distribution for every top glyph and background tile.
- Shifted the bitmap distribution toward sparser lower counts: 1: 16%, 2: 10%, 3: 12%, 4: 24%, 5: 20%, 6: 6%, 7: 5%, 8: 3%, 9: 4%.
- Added independent, phase-shifted power-surge flicker to `INPUT` and `00 // LOCAL`, with irregular dimming and brightness spikes plus a reduced-motion static fallback.
- Synchronized both flickering labels through shared duration and phase variables so they behave as one unstable screen.
- Smoothed the shared power-surge animation with eased brightness, opacity, and glow-radius transitions so the labels fade and swell instead of snapping between states.
- Reduced label surge frequency to roughly 7.5–13 seconds and replaced the top grouped 3×3 glyphs with a 36×3 field of 108 individually timed flickering bits; the background remains composed of weighted 3×3 tiles.
- Added a portable dependency-free fork at `standalone/proof-sigil/` with self-contained HTML, CSS, JavaScript, and usage notes. The project version now independently toggles each of its 108 top bits on/off every second after staggered delays, preserving the shared label power-surges.
- Converted the orange background substrate from grouped 3×3 tiles into a continuous 36×3 field of 108 independently toggling bits while preserving its prior orange opacity and glow treatment; the standalone fork mirrors this representation.
- Added the `/work/web-development/proof-sigil/` case-study page and linked it from the Web Development index. It presents the control as an interface specimen, disassembles its chassis, fields, timing, power surges, and terminal behavior, and includes a live specimen.
- Corrected the background field dimensions to match the original full-area composition: 216 individual bits in a 24×9 grid, with the top signal remaining 108 bits in a 36×3 grid. Both fields expand to their intended regions without added spacing.
- Fixed the doubled top strip by separating the site’s 216-bit 24×9 background loop from its 108-bit 36×3 top loop. Restored the standalone fork to its frozen 108-bit / 36×3 version so subsequent changes remain site-only.
- Added a site-only synchronized outer screen glow to the Proof Sheet button. Its room-lighting aura follows the same slow power-surge clock as `INPUT` and `00 // LOCAL`, with a reduced-motion static fallback; the standalone fork remains unchanged.
- Reworked the site-only screen glow into a dedicated low-opacity aura layer: it stays nearly invisible during normal operation and swells brightly only during the shared text surge, avoiding the previous reversed-feeling brightness.
- Inverted the site-only aura behavior so it glows continuously and dims during surge events, and allowed the workspace overflow needed for the aura to remain visible along the button’s left edge.
- Changed the site-only aura from a filled light wash to a transparent, blurred perimeter halo so the illumination stays on the button edges instead of covering its face.
- Increased the site-only halo’s baseline intensity while narrowing its surge range, keeping the edge glow present and making its flicker variation less dramatic.
- Marked the Input Sigil button complete in the showcase and added a separate finished fork at `standalone/buttontvglow/`. The showcase specimen now mirrors the completed site button, including the 24×9 background, 36×3 signal strip, slower synchronized surges, and edge-only TV aura; the original standalone fork remains independent.
- Matched the Input Sigil showcase button to the completed Proof Sheet button visually: restored the flowing signal-strip layout, exact material and glow values, cursor geometry, hover/focus/pressed states, and reduced-motion behavior. Showcase functionality remains intentionally display-only.
- Corrected the final showcase interaction-state mismatch by restoring Proof Sheet’s inherited hover translation and background-colored text on the Input Sigil specimen.
- Expanded the Input Sigil case study into a fuller disassembly: the five physical/runtime parts now name the individual bit fields and shared aura, and a new potential section describes weather, remote-location, and emergent-language inputs.
- Added live component specimens to the Input Sigil disassembly: chassis, orange substrate field, white signal strip, shared power labels, and terminal typeout are now each shown beside their explanation. Weather remains scoped, not yet connected.
- Locked the Input Sigil weather source to Rovinj, Istria via Open-Meteo current conditions; precipitation, wind, cloud cover, and weather code gently bias bit density and the shared surge period, with local randomness as fallback. Corrected the five demos: removed the stray chassis lamp, restored the signal to a top-aligned 36×3 strip, attached the perimeter aura to the power specimen, centered and repeated the terminal typeout, and added a full-width live 3-row signal between the timing heading and prose.
- Corrected the Input Sigil’s second disassembly regression: the top-bar specimen now generates all 108 bits in the original 36×3 geometry, terminal typing uses slower randomized per-key timing influenced by Rovinj weather, and the timing signal now runs vertically as a glowing 3×36 field between the heading and right-hand prose without a containing border.
- Centered the 36×3 signal specimen inside its disassembly box, slowed the terminal demo further while retaining randomized Rovinj-influenced cadence, and changed the vertical timing field to fixed square pixels with distributed spacing.
- Tightened the vertical timing signal so its fixed square rows sit together as one unified 3×36 form instead of stretching apart across the paragraph height.
- Made the vertical timing signal measure the right-hand prose and generate enough additional 3×N square rows to span it fully, while retaining the unified tight spacing.
- Added a live 72-bit circular signal around “The button does not need to stay alone,” using the same square cells, glow, staggered randomization, and weather bias as the other exposed systems.
- Fixed the circular signal’s scheduler reference so the full ring renders and the page’s staged header typeout continues normally.
- Corrected the ring orbit transform to valid CSS so its 4px cells now distribute around the heading instead of collapsing beneath it.
- Fixed Astro scoping on the dynamically generated ring descendants by applying global selectors to its generated `<i>` holders and `<b>` pixels; built CSS now targets the runtime-created cells.
- Replaced the ring’s collapsing CSS orbit with explicit viewport-relative circle coordinates, so the randomized 4px bits distribute around the heading like the vertical signal rather than stacking at its center.
- Enlarged the potential ring to clear the heading and changed each orbit position into a three-square-wide cluster, matching the signal system’s 3-bit thickness.
- Doubled the potential ring’s orbit density to 144 positions, filling the circumferential gaps without changing the ring radius, square size, or 3-bit thickness.
- Increased the potential ring to 216 positions for a denser, nearly continuous three-bit orbit while preserving its size and individual square geometry.
- Added a no-glow opacity/brightness flicker to the staged `PROVENANCE / 00.1` and `INPUT SIGIL` title lines, synchronized them to the page’s shared clock, and revised the case study to treat Rovinj weather, remote listening, the orbit, and multi-location signal bodies as active systems and new possibilities.
- Reframed the Input Sigil Potential section around provenance research: implemented weather, remote listening, the vertical field, and orbit now live in the general runtime narrative, while Potential introduces three new directions—chain of custody, visible uncertainty, and conservation afterlife.
- Added a terminal typeout for `Rovinj, Istria` in the opening summary, with an attached cursor and reduced-motion fallback.
- Vertically centered the Potential heading and its signal ring against the expanded six-item Potential list.
- Renamed the Input Sigil provenance marker to `PROVENANCE / 00.1` across its accessible label and staged typeout.
- Created `/work/web-development/provenance/` as a dedicated research archive for Proof Sheet and Input Sigil. The web-development index now presents a clear professional practice and links to PROVENANCE as one contained browser-research branch; the former specimen routes remain 308 redirects. The home page now distinguishes selected professional work from the research archive, and the shared shell includes a skip link plus active navigation state.
- Added `/contact/` with direct email, phone, and GitHub channels. Replaced the empty Networking page with a public-safe security and infrastructure positioning page based on proven homelab systems and honest learning boundaries; private resumes and job-search material remain excluded.
- Slice 1 of launch preparation: set `https://montanars.com` as the Astro site URL; centralized Montana Stake title/description fallbacks, canonical URLs, and Open Graph/Twitter tags in `Base.astro`; changed the header wordmark to Montana Stake; recorded the approved no-fixed-job-title identity decision. OG tags intentionally point to `/og.png`, which will be generated in slice 2.
- Slice 2 complete: approved `satori` and `@resvg/resvg-js` are installed as dev dependencies; the build-only OG font sources and licenses are isolated under `src/assets/og-fonts/`; deterministic `/og.png` generation in `src/pages/og.png.ts` emits a 1200×630, 26KB PNG. Full-size and 300px thumbnail checks passed, and no TTF/OTF/WOFF font files leak into `dist/`.
- Completed the approved launch front door: the homepage is now work-first with the exact `View work` and `Contact` actions, showing First Known Use for artists/clients and Security + Systems for employers; PROVENANCE remains nested under Web Development. Primary navigation now contains Web development, Security + Systems, and Contact; Audio and Fabrication remain routable but hidden until finished work is ready.
- Added launch discoverability and recovery surfaces: a custom 404 page, canonical `robots.txt`, and a sitemap covering the homepage, contact, Web Development, PROVENANCE, Security + Systems, Audio, and Fabrication routes.
- Added a 3×3 Input Sigil SVG favicon and a Cloudflare Pages `_headers` baseline covering clickjacking, MIME sniffing, referrer leakage, unnecessary browser capabilities, and HTTPS transport. Cloudflare did not emit the attempted cross-origin isolation headers from static Pages headers, so they were removed rather than treated as active. CSP remains a scoped follow-up because the site currently uses inline scripts, WASM workers, and an external iframe.
- Completed the first audit response: the First Known Use case study now clearly separates its minimal deployed WIP from the broader planned archive; the contact page no longer presents a `mailto:` form; the global shell has a utility footer; the homepage exposes three proof surfaces; Proof Sheet documents its browser-local data boundary; and the wildcard CORS header is explicitly detached at the Pages layer. CSP remains scoped for a Pages Function or edge-rule implementation after the static `_headers` path failed to emit a report-only policy in the live response.
- Removed the duplicate `No form? Direct line.` prompt from the contact page; the form now leads directly into one concise direct-contact section.
- Browser reproduction found the actual first failure was `Failed to construct 'URL': Invalid base URL` in the worker WASM path setup. The worker now resolves imported Vite asset URLs against `self.location.origin`; the browser trace then confirmed successful Transformers.js, OWLv2 metadata/model, local `.mjs`, and local `.wasm` requests. WASM uses q8 while WebGPU retains q4f16, and detector errors now identify worker, local runtime, model initialization/download, or inference stages.
- Chromium fixture benchmark on `proof-sheet-1050026430.png`: the full 20-query WASM q8 run retained detections in 46,387ms (19,406ms model load, 25,443ms inference); the reduced five-query run retained detections in 38,205ms (11,669ms model load, 25,479ms inference). Transformers.js resolves q8 to `onnx/model_quantized.onnx` (not a literal `model_q8.onnx`). The default CPU vocabulary is now five broad queries, inference is downscaled to 512px, and a 90-second timeout exits to labeled geometry fallback. Detection is cached across seed recomposition. A headless SwiftShader WebGPU attempt reached the model stage but failed initialization with runtime code `267935216`; the WASM path remains the verified fallback.
- Refreshed the generated Open Graph preview card to match the current Montana Stake launch navigation and research archive labels.
- Added a two-stage home-lab systems diagram to Security + Systems: operational use feeds a provenance loop covering decisions, changes, evidence, context, and recovery.
- Refined the provenance loop so green is limited to its active decision signal and directional arrow; the orbit and supporting labels use the shared site treatment.
- Connected both home-lab loops through a shared `RECOVER` state and a labeled provenance handshake, distinguishing system recovery from recovery of the reasoning and evidence around it.
- Corrected the composite map to use explicit tangent circles with all orbit nodes placed on their geometry; shortened the explanatory copy beneath it.
- Evened both tangent loops to five equally spaced points, using the single shared `RECOVER` node as the fifth point on each circle.
- Added CSS/SVG signal lights that travel around both loop edges, glow at the shared recovery interface, and freeze to a static highlight under reduced-motion preferences.
- Removed the redundant direction arcs between the upper nodes and warmed the traveling signals with a broader, softer SVG bloom.
- Reduced the traveling signal core to the base orbit weight and moved its visual emphasis into a much larger blurred halo.
- Extended each traveling signal into offset halo, mid-glow, and core layers so it covers more of the circle with tapered luminous ends.
- Changed the circles to a muted baseline with a layered moving trail: a long low-opacity wake, mid halo, glow, and thin core now illuminate the circumference and fade as the signal passes.
- Extended the moving wake to a faint 70% field plus shorter trail layers so the illumination visibly fills more of each circle before decaying back to the muted baseline.
- Shortened the wake again and synchronized each orbit node as a signal receiver: its ring and center dot briefly bloom in the loop color as the light reaches it, with all node animation disabled for reduced motion.
- Corrected the green loop receiver phases for its half-cycle signal offset and aligned the shared recovery pulse to the moment both lights cross the tangent node.
- Shifted receiver pulses into the midpoint of the trailing wake and matched the moving core to the base orbit’s thin, squared line treatment.
- Tightened the wake again: shorter dash lengths and smaller halo widths preserve the node timing while reducing the signal footprint.
- Thinned the moving core to 1px, delayed receiver pulses by 400ms, and added synchronized fill/glow animation to the shared recovery core and dot.
- Reordered the SVG paint layers so the muted orbit line traces over the colored core while preserving its halo, and strengthened the shared recovery ball’s direct fill, dot, stroke, and glow pulse.
- Removed the unexplained filled states from Configure and Decision, advanced the shared recovery pulse slightly, and increased only the blurred signal layers for a brighter glow.
- Made the systems chart mobile-safe with a contained, keyboard-focusable horizontal viewport, touch scrolling, visible focus treatment, and a reduced mobile canvas width.
- Replaced the clipped mobile scroll composition with a dedicated vertical tangent-loop SVG: both circles and the single shared recovery node now fit the phone viewport without horizontal scrolling while retaining signal and node animations.
- Corrected the responsive display rules so desktop and mobile SVG compositions are mutually exclusive; the mobile viewport now renders only the vertical chart.
- Gave the mobile tangent loops their own signal start point and receiver phases so both mobile lights meet the shared recovery node in sync.
- Advanced non-recovery receiver pulses by 600ms so node blooms sit in the center of each wake; the shared `RECOVER` timing remains unchanged.
- Delayed the shared recovery ring, core, and dot by 200ms for a final timing alignment.
- Production deployment verified through Wrangler Pages direct upload: `a4cc0139` at `a4cc0139.montanars.pages.dev`, built from `a9a0a81`.
- Creative direction is intentionally open; no palette, typography, layout, or project taxonomy is locked.
- Portfolio scope includes any kind of project, not only web development.
- Initial technical direction agreed: Astro 7 as the semantic shell, repository-authored content, and no CMS.
- Interactive work may use unrestricted JavaScript, p5.js, Canvas, WebGL/WebGPU, audio, framework islands, games, or real-time features when a project calls for them.
- Experiments should remain isolated and reversible, with accessible fallbacks; server infrastructure is deferred until a concrete project requires it.
- Codex guidance optimized for lower-capability models: centralized site-specific decision rules, selective context loading, explicit scaffold gate, compact validation matrix, and role-specific builder/reviewer deltas.

## Next

- Site is live. The eight-graphic visual review pass (2026-08-04) is complete: Practice relay, Trust-boundary topology, Render pipeline, and Boundary plate were all reviewed and fixed; the custody ribbon and specimen marks/archive-layer/route-trace slices were reviewed and three were removed by Montana's call rather than refined (see DECISIONS.md).
- First Known Use is now a themed "tour" of the live firstknownuse.com site (own xerox/fluorescent theme, scoped to that page) rather than a conventional case study; still needs the night-mode toggle question resolved if Montana wants the tour to react to the live site's theme (requires a small change in the firstknownuse repo — skipped for now).
- Add real projects to each remaining section (audio, fabrication) without forcing them through the web-development presentation or a shared project schema.
- Repeat the Proof Sheet browser smoke test with a representative image after WASM CPU inference completes; a headless WASM run had not yet returned detections within the test window as of the last check.
- Test the First Known Use preview-window transition in a supporting browser, a non-supporting browser, and with reduced motion enabled.
