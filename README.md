# Portfolio

Broad spectrum portfolio build for all of Montana's things and projects.

The creative direction is deliberately open; use `SPEC.md` as the flexible product boundary, then develop visual
and content slices before committing to reusable systems.

See the Codex instructions in `AGENTS.md`, shared conventions in `../../AGENTS.md`, and the Codex workflow in
`../../docs/CODEX-WORKFLOW.md`. `CLAUDE.md` remains available for Claude-compatible sessions.
