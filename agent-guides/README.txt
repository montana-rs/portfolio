These builder and reviewer guides contain only role-specific instructions. `AGENTS.md` remains the shared authority so rules are not duplicated into every agent context.

The source template is `../../templates/site-starter/agent-guides/`.
